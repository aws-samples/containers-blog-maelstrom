apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: agentcore-memory.fsi.aws.example.com
  labels:
    provider: aws
    kind: agentcore-memory
spec:
  compositeTypeRef:
    apiVersion: fsi.aws.example.com/v1alpha1
    kind: XAgentCoreMemory
  writeConnectionSecretsToNamespace: crossplane-system
  resources:
    - name: memory
      base:
        apiVersion: bedrockagentcore.aws.upbound.io/v1beta1
        kind: Memory
        spec:
          forProvider:
            eventExpiryDuration: 30
          providerConfigRef:
            name: default
          deletionPolicy: Delete
      patches:
        - fromFieldPath: spec.parameters.region
          toFieldPath: spec.forProvider.region
        - fromFieldPath: spec.parameters.name
          toFieldPath: spec.forProvider.name
        - fromFieldPath: spec.parameters.description
          toFieldPath: spec.forProvider.description
        - fromFieldPath: spec.parameters.eventExpiryDuration
          toFieldPath: spec.forProvider.eventExpiryDuration
        - fromFieldPath: spec.parameters.tags
          toFieldPath: spec.forProvider.tags
        - fromFieldPath: metadata.labels
          toFieldPath: metadata.labels
        # Publish AgentCore IDs to the XR status so they land in the
        # connection secret (connectionDetails below).
        - type: ToCompositeFieldPath
          fromFieldPath: status.atProvider.id
          toFieldPath: status.id
        - type: ToCompositeFieldPath
          fromFieldPath: status.atProvider.arn
          toFieldPath: status.arn
      connectionDetails:
        - name: id
          type: FromFieldPath
          fromFieldPath: status.atProvider.id
        - name: arn
          type: FromFieldPath
          fromFieldPath: status.atProvider.arn
        - name: name
          type: FromFieldPath
          fromFieldPath: status.atProvider.name
